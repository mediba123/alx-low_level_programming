#include "main.h"
/**
 *_isdigit - check for a digit (0 through 9)
 * @c: interger should be check
 *Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');

}

